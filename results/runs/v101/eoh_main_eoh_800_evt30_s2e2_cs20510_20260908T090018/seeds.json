[
 {
  "algorithm": "The novel score function assigns a higher priority to bins that, after placing the item, have the remaining capacity closest to a predefined target threshold, promoting compact bin usage and reducing total bins.",
  "code": "def score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    target_threshold = bins.mean()  # or define it dynamically as needed\n    return -np.abs((bins - item) - target_threshold)"
 },
 {
  "algorithm": "Assign the item to the bin with the highest score, calculated as the inverse of the ratio between the item size and the bin's remaining capacity, raised to a power that increases the preference for bins with tighter fits.",
  "code": "def score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current current_item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    alpha = 2  # exponent to amplify the preference for tighter fits\n    return 1 / ((item / bins) ** alpha)"
 },
 {
  "algorithm": "The novel score function assigns higher priority to bins that, after placing the item, are closest to their new capacity, promoting balance and reducing the number of bins used.",
  "code": "def score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    return (bins - item) / (bins + item)"
 },
 {
  "algorithm": "The algorithm scores bins based on how closely their remaining capacity matches the item size, prioritizing bins that are just large enough to fit the item, thus promoting tight packing and reducing the number of bins used.",
  "code": "import numpy as np\n\ndef score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    return 1 / (1 + (bins - item))"
 },
 {
  "algorithm": "\"description\": \"The algorithm assigns each item to the bin with the highest score, where the score is calculated as the inverse of the remaining capacity after adding the item, encouraging tight packing and minimizing extra space.\"",
  "code": "def score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    return 1 / (bins + item)"
 },
 {
  "algorithm": "The algorithm scores bins using a combination of remaining capacity and a decreasing preference based on the position of the bin, encouraging reuse of earlier bins to promote tighter packing and reduce the total number of bins used.",
  "code": "import numpy as np\n\ndef score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    # Remaining capacity after placing the item\n    remaining = bins - item\n    # Penalty based on bin index to prefer earlier bins\n    index_penalty = 1 / (np.arange(len(bins)) + 1)\n    # Score is a combination of remaining capacity and index preference\n    scores = remaining * index_penalty\n    return scores"
 },
 {
  "algorithm": "The algorithm scores bins by computing the ratio of the item size to the bin's remaining capacity and then subtracts it from 1 to prioritize bins where the item fits best without excessive leftover space.",
  "code": "import numpy as np\n\ndef score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    return 1 - (item / (bins + 1e-9))"
 },
 {
  "algorithm": "The novel score function prioritizes bins by combining their remaining capacity and penalizing bins with higher frequency of item allocations to encourage balanced bin usage.",
  "code": "import numpy as np\n\ndef score(item: int, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Score each bin for assigning the current item. Higher score = preferred bin.\n\n    Args:\n        item: size of the current item to assign\n        bins: remaining capacities of feasible bins (all >= item size)\n    Returns:\n        scores: priority scores for each bin\n    \"\"\"\n    remaining_capacity = bins - item\n    score_values = remaining_capacity / (np.abs(np.log(bins)) + 1e-9)\n    return score_values"
 }
]